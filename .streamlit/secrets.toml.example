# Copy to secrets.toml for local dev (NEVER commit secrets.toml)
# In Streamlit Community Cloud, set these in Settings → Secrets.

# ENTSO-E Transparency Platform — register for free at
# https://transparency.entsoe.eu/  → My Account Settings → Generate token
ENTSOE_TOKEN = "your-entsoe-token-here"

# GIE AGSI+ — register for free at https://agsi.gie.eu/account
AGSI_TOKEN = "your-agsi-token-here"
